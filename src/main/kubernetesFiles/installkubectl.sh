ReadyMade kubernetes cluster:
===============================================

On GCP search for kubernetes clusters

Click create → click on switch to standard cluster
A cluster with 3 worker nodes will be created.
Click on Connect to connect to the cluster

Connect to cluster via command line -> copy the command and click RUn in cloud shell
